from Base import all

type Body

    type Body bytes

    to_text : Text
    to_text = Text.from_utf_8 this.bytes

    # TODO [DB] return JSON when it's implemented.
    to_json : Any
    to_json = this.text

    to_bytes : Vector
    to_bytes = this.bytes
