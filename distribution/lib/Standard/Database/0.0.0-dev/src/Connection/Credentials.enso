from Standard.Base import all

from project.Connection.Credentials.Credentials import Username_And_Password
from project.Connection.Credentials.Credentials export Username_And_Password

type Credentials
    ## Simple username and password type.
    Username_And_Password username:Text password:Text

    ## Override `to_text` to mask the password field.
    to_text : Text
    to_text self = 'Credentials ' + self.username + ' *****'
