private

from Standard.Base import all


type Spec
    Impl (name : Text) (code : Any -> Any)

    to_text self = "Spec '" + self.name + "'"

    run self =
        IO.println <| "Running spec " + self.to_text + "..."
        self.code Nothing
