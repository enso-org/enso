from Standard.Base import all
from Standard.Base.Metadata import Display, make_single_choice, Widget

## PRIVATE
   Type to specify the maximum number of rows or unlimited.
type Row_Limit
    ## Allow unlimited.
    No_Limit

    ## Limit to a specific number of rows.
    Limit n:Integer=100

## PRIVATE
Row_Limit.from (that:Nothing) =
    _ = that
    Row_Limit.No_Limit

## PRIVATE
Row_Limit.from (that:Integer) =
    Row_Limit.Limit that
