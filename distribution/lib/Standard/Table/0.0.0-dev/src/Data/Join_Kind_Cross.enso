from Standard.Base import all

type Join_Kind_Cross
    ## Cartesian product: each row of the left table is paired with each row of
       the right table.
    Cross

    ## PRIVATE
       Returns the SQL representation of this join kind as text.
    to_sql : Text
    to_sql self = "CROSS JOIN"
