/**
 * @file Helper functions related to React.
 * @deprecated Don't use forwardRef anymore.
 */
import { forwardRef } from 'react'

export {
  /**
   * @deprecated Don't use forwardRef anymore.
   */
  forwardRef,
}
