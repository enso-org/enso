{
  "extends": "../../tsconfig.json",
  "include": ["src/**/*.ts", "src/**/*.d.ts"],
  "exclude": ["parser-codegen"],
  "compilerOptions": {
    "composite": true,
    "isolatedModules": true,
    "noEmit": false,
    "rootDir": "src",
    "outDir": "dist",
    "types": ["node", "vitest/importMeta"]
  }
}
