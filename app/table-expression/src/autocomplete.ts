import type { CompletionContext } from '@codemirror/autocomplete'

/** Completion provider for the Table Expression DSL. */
export function completions(_context: CompletionContext) {
  // TODO (#12305)
  return null
}
