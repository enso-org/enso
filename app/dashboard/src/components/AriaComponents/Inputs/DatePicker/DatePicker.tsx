import * as aria from "#/components/aria"

/** Props for a {@link DatePicker}. */
export interface DatePickerProps {
	
}

/**  */
export default function DatePicker(props: DatePickerProps) {
	const {  } = props
	return <aria.DatePicker></aria.DatePicker>
}