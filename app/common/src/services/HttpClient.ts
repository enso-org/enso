/**
 * @file empty on purpose, in order to replace previously removed file that sticks around in caches and often causes
 * build issues. Can be removed after a while, once all builders pick this one up.
 */
