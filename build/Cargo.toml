[package]
name = "enso-build3"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
enso-build = { path = "script/build" }
enso-build-cli = { path = "script/cli" }
ide-ci = { path = "script/ci_utils" }
