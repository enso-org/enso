[package]
name = "fuzzly"
version = "0.1.0"
authors = ["Enso Team <contact@enso.org>"]
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
derivative = { workspace = true }
enso-text = { path = "../text" }

[dev-dependencies]
rand = "0.8.5"
rand_chacha = "0.3.1"
