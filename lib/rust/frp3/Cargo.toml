[package]
name = "enso-frp3"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["stack-trace"]
stack-trace = []

[dependencies]
bumpalo = { version = "3.12.0" }
derivative = "2.2.0"
slotmap = "1.0.6"
smallvec = "1.10.0"
frp-macros = { path = "frp-macros" }
enso-prelude = { path = "../prelude" }


[[example]]
name = "one"