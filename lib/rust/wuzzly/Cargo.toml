[package]
name = "wuzzly"
version = "0.1.0"
authors = ["Enso Team <contact@enso.org>"]
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
derivative = { workspace = true }

[dev-dependencies]
rand = "0.8.5"
rand_chacha = "0.3.1"
