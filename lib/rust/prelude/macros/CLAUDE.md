# enso-macros

Proc-macro half of `enso-prelude`. Hosts derives and function-like macros that
can't live in a normal `rlib`. Depends on `enso-macro-utils` for shared `syn`
helpers.

Published to crates.io alongside `enso-prelude` (see `version = "0.2.7"` — keep
the two versions aligned when bumping).
