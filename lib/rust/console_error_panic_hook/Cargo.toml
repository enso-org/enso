[package]
authors = ["Nick Fitzgerald <fitzgen@gmail.com>", "Enso Team <contact@enso.org>"]
categories = ["wasm"]
description = "A panic hook for `wasm32-unknown-unknown` that logs panics to `console.error` and Electron"
license = "Apache-2.0/MIT"
name = "enso_console_error_panic_hook"
readme = "./README.md"
version = "0.1.6"

[dependencies]
enso-debug-api = { path = "../debug-api" }
cfg-if = "1.0.0"
wasm-bindgen = "0.2.37"
