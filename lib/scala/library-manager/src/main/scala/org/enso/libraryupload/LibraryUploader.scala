package org.enso.libraryupload

import java.nio.file.Path
import scala.util.Try

object LibraryUploader {
  def uploadLibrary(projectRoot: Path, authToken: auth.Token): Try[Unit] = Try {
    ???
  }
}
