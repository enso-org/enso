package org.enso.gateway

import org.enso.gateway.protocol.Id

object TestMessageDefinitions {
  val clientName    = "Enso IDE"
  val clientVersion = "1.0"
  val serverName    = "Enso Language Server"
  val serverVersion = "1.0"
  val id1           = Id.Number(1)
  val id2           = Id.Number(2)
}
