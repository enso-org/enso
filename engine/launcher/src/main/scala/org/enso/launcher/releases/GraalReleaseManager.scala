package org.enso.launcher.releases

object Placeholder3
/*
import java.nio.file.Path

import nl.gn0s1s.bump.SemVer
import org.enso.launcher.releases.github.GithubReleaseProvider

import scala.annotation.unused

class GraalReleaseManager(@unused releaseProvider: ReleaseProvider) {
  def downloadPackage(@unused version: SemVer, @unused path: Path): Unit = ???
}

object GraalReleaseManager
    extends GraalReleaseManager(
      new GithubReleaseProvider("graalvm", "graalvm-ce-builds")
    )
 */
