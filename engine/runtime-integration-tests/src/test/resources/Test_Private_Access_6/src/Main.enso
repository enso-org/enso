from local.Test_Private_Access_1 import My_Type

main =
    # Get the private constructor via a method - this is valid. `func` is now a
    # lambda function that invokes the private constructor
    priv_ctor_fn = My_Type.get_ctor
    # This is valid - the actual private constructor is called in the project
    # Test_Private_Access_1, not in this project.
    obj = priv_ctor_fn 42
    obj.get_data
