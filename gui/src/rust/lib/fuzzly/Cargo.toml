[package]
name = "fuzzly"
version = "0.1.0"
authors = ["Enso Team <contact@enso.org>"]
edition = "2018"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
enso-prelude = { path = "../../../../../lib/rust/prelude" }
