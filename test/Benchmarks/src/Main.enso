import project.Vector.Distinct
import project.Vector.Operations

collect_all =
    vec_ops = Operations.all
    vec_distinct = Distinct.collect_benches
    [vec_ops, vec_distinct]

main =
    collect_all.each suite->
        suite.run_main
