This is a set of tests for the Google integration for Enso.

## Testing Google Sheets

To run the tests, you need to supply credentials for Google Sheets

You need to log in to your Enso Cloud account that you are running the tests against and create a new Google credential with `Sheets` scope which must be called `GoogleSheets`.
