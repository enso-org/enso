name: Cloud_Tests
namespace: enso_dev
enso-version: default
version: 0.0.1
license: MIT
author: enso-dev@enso.org
maintainer: enso-dev@enso.org
# Cloud_Tests import stuff from Base_Tests sibling project. So we need this property here.
prefer-local-libraries: true
# Run in JVM mode due to hacky access to Standard.Base Java classes
jvm: true
