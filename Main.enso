from Standard.Base import all

import Standard.Base.System.Process

example = 
    builder = Process.new_builder "echo"
    builder.set_arguments ["hello, world!"] . create

main =
    result = here.example
    IO.println result
