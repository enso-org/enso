from Standard.Base import all

import Standard.Base.Data.Map
import Standard.Examples

example = Examples.map.fold_with_key "" (l -> k -> v -> l + k.to_text + v)

main =
    result = here.example
    IO.println result

