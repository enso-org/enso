//! The driver for the Enso parser.



// ==============
// === Parser ===
// ==============

/// The Enso parser itself.
#[derive(Clone,Debug,Default,Eq,PartialEq)]
pub struct Parser;

impl Parser {
    /// Constructor.
    pub fn new() -> Self {
        Self
    }
}
